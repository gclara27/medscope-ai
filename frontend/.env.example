# Frontend (Vite) — copy to frontend/.env for local overrides
# Reference: docs/Environment/Environment.md §4

# Empty = relative API URLs (Vite dev proxy → localhost:8000, or nginx proxy in Docker)
VITE_API_BASE_URL=

# Production example (API on separate host):
# VITE_API_BASE_URL=https://api.your-hospital.example
