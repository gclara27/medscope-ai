/**
 * Clinical risk palette (RUX-011, design-system.light.md).
 * Shared by Tailwind and application code — do not duplicate hex values elsewhere.
 */
export const RISK_COLORS = {
  low: "#16a34a",
  medium: "#f59e0b",
  high: "#dc2626",
};

/** surface-container-high — gauge track (non-risk decorative). */
export const RISK_GAUGE_TRACK_COLOR = "#e7e8e9";
